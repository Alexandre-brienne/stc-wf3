{% extends 'base.html.twig' %}


{% block body %}
<div class="message">
	{% for messagerie in messageries %}
		
			<p>{{ messagerie.expediteur.username }} {{ messagerie.message }}</p>

    

	{% else %}
           <p>oh je suis dessolé mais il y'a pas de message avec cette personne :(</p>         
	{% endfor %}
</div>





{{ form_start(form) }}
    {{ form_widget(form) }}
    {# <button class="btn btnjsfff">{{ button_label|default('Save') }}</button> #}
{{ form_end(form) }}
<button class="btn btnjs">Envoiyer</button>
    <pre>
        {{ dump(messageries) }}
    </pre>

    <pre>
        {{ dump(userid) }}
    </pre>
<style>
    .message{
        
        color: red;

    }
</style>
<script> 
var userid = {{ userid }} 
var urlajax = "{{ path('sendmessage') }}";

</script>
<script src="{{asset('js/message.js')}}"></script>
{% endblock %}