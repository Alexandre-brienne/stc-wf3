{# <form method="post" class="form-register">

	<div>
		<div>
			<label id="register-title" for="inputUsername">Pseudo</label>
            <input type="text" placeholder="Pseudo" value="{{ last_username }}" id="inputUsername" name="username" required autofocus>

		</div>
		<div>
			<label id="register-title" for="inputPassword">Mot de Passe</label>
		<input type="password" placeholder="Mot de passe" id="inputPassword" name="password">
        <input type="hidden" name="_csrf_token" value="{{csrf_token('authenticate')}}">
		</div>
	</div>
	<!-- Valider connexion -->
	<button type="submit" id="connecté">Connexion</button>
</form> #}
{# <div class="login-container">
	<form method="POST">
		{% if error %}
			<div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
		{% endif %}

		{% if app.user %}
			<div class="logininfomation">
				<p>Vous etes connecté
					{{ app.user.username }}
				</p>
				<a href="{{ path('app_logout') }}">Se deconnecter</a>
			</div>

		{% else %}
			<input type="text" placeholder="Pseudo" value="{{ last_username }}" id="inputUsername" name="username" required autofocus>
			<input type="password" placeholder="Mot de passe" id="inputPassword" name="password">
			<input type="hidden" name="_csrf_token" value="{{csrf_token('authenticate')}}">
			<button type="submit">connexion</button>
   
		{% endif %}


	</form> #}
