<!DOCTYPE html>
<html lang="fr">

	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">


		<link rel="stylesheet" href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">

		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
		<link rel="stylesheet" href="css/normalize.css">
		<link rel="stylesheet" href="{{asset('css/profil_membre.css')}}">


		<title>Profil Membre</title>
	</head>

	<body class="profil_body">
			<header>
			{% block header %}
				<div class="profil">
					<div class="info_profil">
						<p>{{app.user.username}}</p>
						<p>{{app.user.point}}
							GeekCoin</p>
					</div>
					{% if app.user.imageprofil != null %}
						<img src="{{ asset('uploads/' ~ app.user.imageprofil)}}" alt="profil1" class="photoprofil">
					{% else %}
						<img src="{{asset('image/profil.png')}}" alt="photo de profil" class="photoprofil">

					{% endif %}

				</div>

				<div>
					<div class="collapse" id="navbarToggleExternalContent">
						<div class="bg-transparent p-4">
							<h5 class="text-light h4">Collapsed content</h5>
							<span class="text-muted">Toggleable via the navbar brand.</span>
						</div>
					</div>
					<nav class="navbar navbar-dark bg-transparent position-absolute bottom-0 end-0">
						<div class="container-fluid">
							<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggleExternalContent" aria-controls="navbarToggleExternalContent" aria-expanded="false" aria-label="Toggle navigation">
								<span class="navbar-toggler-icon"></span>
							</button>
						</div>
					</nav>

				</div>
				<nav class="menu_profil">

					<a href="#"><img src="{{asset('image/logo_geekandstart.png')}}" alt="logo GaS" class="logo"></a>

					<section class="liste">

						<ul>
							<li>
								<a href="{{ path('profil', {'username': app.user.username})}}" class="onglet">Mon Profil<a></li>
									<li>
										<a href="{{ path('boitemessagerie',{'username': app.user.username})}}" class="onglet">Messagerie</a>
									</li>
									<li>
										<a href="{{ path('app_logout')}}" class="deconnexion">Déconnexion</a>
									</li>
								</ul>
							</li>
						</section>
					</nav>
				{% endblock %}
			</header>


			<main class="profil_main_info">

				<section>
					<div class="profil_info_membre">

						<img src="{{ asset('uploads/' ~ user.imageprofil)}}" alt="photoprofil" class="photo_membre">

						<div class="profil_button">
							{% if app.user.username == user.username %}
								<a href="{{ path('profil_edit', {'username': user.username}) }}">
									<button type="button" class="btn btn-light">
										<span class="profil_modif">Modifier le profil</span>
									</button>
								</a>
							{% elseif app.user.username != user.username  %}


								<a href="{{ path('messagerieduo', {'username': app.user.username,'iduser':user.id})}}">
									<button type="button" class="btn btn-light">
										<i class="las la-envelope profil_i"></i>
									</button>
								</a>
							{% endif %}
						</div>
						<div class="profil_identite">
							<div class="profil_prenom">
								<div>
									<p class="profil_name">{{ user.username }}</p>
									<p>{{ user.ville }}</p>
									<p>{{ user.sexe }}</p>
								</div>

							</div>
							<p>profession :
								{{ user.profession }}<</p><p>Département:{{user.dapartement}}</p><p>SituationAmoureuse:{{user.situation}}</p><p>Taille:{{user.taille}}cm</p><p>Cheveux:{{user.cheveux}}</p><p>SigneAstrologique:{{user.astrologique}}</p><p>Recherche:{{user.recherche}}</p></div><divclass="profil_interets"><h2class="profil_titre_interets">Centresd'intérêts</h2><divclass="profil_preferences"><div><p>{{dump(user)}}</p></div></div></div></div></section></main><footer></footer></body></body></html></p>
